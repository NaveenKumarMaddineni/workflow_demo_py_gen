from setuptools import setup, find_packages

setup(
    name='pygen',
    version='0.1.0',
    description='Python code generator from config',
    author='Your Name',
    packages=find_packages(),
    python_requires='>=3.7',
)
